import type { DiscoverGateway } from '../discovery/index.js';
import type { Client, InternalMapOptions } from '../types.js';
import type { AbortOptions } from 'abort-error';
export declare class UPNPClient implements Client {
    private closed;
    private readonly discoverGateway;
    private readonly shutdownController;
    static createClient(discoverGateway: () => DiscoverGateway): UPNPClient;
    constructor(discoverGateway: () => DiscoverGateway);
    map(localPort: number, options: InternalMapOptions): Promise<number>;
    unmap(localPort: number, options: InternalMapOptions): Promise<void>;
    externalIp(options?: AbortOptions): Promise<string>;
    private findNamespacedKey;
    private findGateway;
    close(): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map
import { APIProvider } from './types.js';
export declare class APIError {
    message: string;
    status: number;
    constructor(message: string, status: number);
    toString(): string;
}
export declare const api: APIProvider;
